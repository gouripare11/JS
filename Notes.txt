DATA TYPES

1. Primitive - no, string(' ' OR " "), boolean
2.Special - null (js m null ko obj maana jata h)
3. Non- premitive - arr, func, obj

Priority order : String > number > 
-----------------------------------------------------------------------------------------------------

BOM - interaction between browser and window

alert - warning (ok)
confirm -  (ok/cancel) return true/false
prompt - i/p from users
-----------------------------------------------------------------------------------------------------
VARIABLE DECLARATON 

let, var, const
- re-declaration of keywrd in var is allowed but not in let and const
-----------------------------------------------------------------------------------------------------
HOISTING

var - support, not in let, const
if you declare var then it automatically shifts to top of block and shows undefined but let/const shows error

DOM(Document Object Modeling)
tree like structure of HTML where we can change the elements dynamically

onclick()- for button
-------------------------------------------------------------------------------------------------------
STRING METHODS

length, slice(), concat(), substring(), replace()[replace only one], toUpperCase(), toLowerCase(), trim()[remove starting and ending white spaces of string]
replaceAll(), charAt(), charCodeAt(), split()[str to array], indexof()
--------------------------------------------------------------------------------------------------------
TEMPLATE LITERALS

supports both double quotes ans single quotes in same string using *back-ticks* i.e - `jnnnon`
supports multiple line of strings
can use variable in ${   } to show values without concatination 
--------------------------------------------------------------------------------------------------------
ARRAYS

let a=new Array(1,2,3) or let a=[1,2,3,4]
methods: length, pop() removes last element, push() adds to last of array, shift() removes last element, unshift() add new element to beginning
---------------------------------------------------------------------------------------------------------
ARRAY TO STRING
toString() //any datatype to string
joint()    //
typeof()   // to check data type













